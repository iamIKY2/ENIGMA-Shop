<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tieuluan"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!-- Không có intent-filter ở đây để không phải là launcher -->
        </activity>
        


        <activity android:name=".ProductDetailActivity" />
        <activity android:name=".LaptopActivity" />
        <activity android:name=".man_hinh" />
        <activity android:name=".PhoneActivity" />
        <activity android:name=".TabletActivity" />
        <activity android:name=".WatchActivity" />
        <activity android:name=".TVActivity" />
        <activity android:name=".AudioActivity" />
        <activity android:name=".AccessoryActivity" />
        <activity android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>